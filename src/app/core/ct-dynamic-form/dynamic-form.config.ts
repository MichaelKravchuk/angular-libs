import { DynamicFormConfig } from '../../../../projects/ct-dynamic-form/src/lib/dynamic-form.config';
import { TextField, TextFieldComponent } from './fields';

export const DYNAMIC_FORM_CONFIG: DynamicFormConfig = [
    { component: TextFieldComponent, config: TextField }
];
